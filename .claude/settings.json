{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -Command \"$files = @(git diff --name-only --diff-filter=AM -- '*.ps1' '*.psm1'); if ($files.Count -gt 0) { foreach ($f in $files) { Invoke-ScriptAnalyzer -Path $f -Severity Warning,Error } } \""
          }
        ]
      }
    ]
  }
}
